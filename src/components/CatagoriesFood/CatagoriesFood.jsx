import React, { use } from 'react';

const CatagoriesFood = ({ manageMenuExplore }) => {
  const handleExploreClick = use(manageMenuExplore);
  console.log("handleExploreClick", handleExploreClick);
  return (
    <div>
      
    </div>
  );
};

export default CatagoriesFood;
